# Example agent config (~/.relay/config.yaml)
# Copy and adjust; never commit real tokens.

relay_url: http://127.0.0.1:8740
# relay_url: https://example.com
# relay_url: http://127.0.0.1:8740

device_id: ""
device_token: ""
profile: ""          # auto-detect if empty
targets: []          # auto-detect if empty
allow_live_writes: false
agent_version: 0.1.0
